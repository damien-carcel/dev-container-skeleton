services:
  devcontainer:
    build: ./docker/devcontainer
    command:
      - sleep
      - infinity
    user: devcontainer
    volumes:
      - type: bind
        source: .
        target: /srv/app
    working_dir: /srv/app
