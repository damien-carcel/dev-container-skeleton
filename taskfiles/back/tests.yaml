version: 3

tasks:
  static:code:lint:
    aliases: [c:l]
    cmds:
      - docker run --rm --mount type=bind,source=$(pwd),target=/app --workdir /app/api/hello golangci/golangci-lint:v1.59.1 golangci-lint run -v
    desc: Lint the application code
